[project]
name = "tonywebb"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "llm>=0.31",
    "llm-anthropic>=0.1",
    "llm-gemini>=0.1",
    "llm-ollama>=0.16.0",
    "requests>=2.33.1",
    "surya-ocr>=0.14.0",
    "transformers>=4.40.0,<5.0.0",
    "pytesseract>=0.3.13",
    "pillow>=10.4.0",
]

[project.scripts]
tonywebb = "tonywebb.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["tonywebb"]

[dependency-groups]
dev = [
    "pytest>=8.0",
]
